# Prom Pulse remaining fixes

This build contains the frontend fixes for:
- Secret Crush toggle UI compatibility
- Declined + unmatched list
- Removing the failed 60-second-match popup

Run `supabase/migrations/20260824_remaining_feature_fixes.sql` once in the Supabase SQL Editor for the database-side fixes:
- `save_secret_crush` toggle
- `cancel_prom_request`
- safe re-request after decline/cancel/unmatch
- `unmatch_prom`
